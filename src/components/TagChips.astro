---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{tags.length > 0 && (
  <div class="flex flex-wrap gap-1.5">
    {tags.map((tag) => (
      <a href={`/topics/${tag}`} class="tag-chip">
        {tag}
      </a>
    ))}
  </div>
)}